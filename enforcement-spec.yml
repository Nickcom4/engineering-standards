# ESE Enforcement Specification
# Auto-generated by scripts/generate-enforcement-spec.sh
# Source: STANDARDS.md, 7 addenda, adoption.md, templates, starters
# Do not edit by hand — regenerate with: bash scripts/generate-enforcement-spec.sh generate
# Generated: 2026-04-11T17:30:24Z
#
# Total gates: 313

version: "1.0"
generated: "2026-04-11T17:30:24Z"
gate_count: 313
gates:
  - id: REQ-1.1-01
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: A problem statement is documented before design or implementation begins.
    source: STANDARDS.md
  - id: REQ-1.1-02
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "A first principles check (3 questions: fundamentally doing, constraints, simplest solution) is completed before design begins."
    source: STANDARDS.md
  - id: REQ-1.1-03
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Explicit IN scope and OUT of scope lists are present in the work item or scoping document.
    source: STANDARDS.md
  - id: REQ-1.1-08
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Each success metric has a numeric threshold or binary pass/fail condition.
    source: STANDARDS.md
  - id: REQ-1.1-11
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Each success metric states when it will be measured.
    source: STANDARDS.md
  - id: REQ-1.1-12
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Each success metric states the measurement method.
    source: STANDARDS.md
  - id: REQ-1.1-13
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: The problem statement identifies who has the problem.
    source: STANDARDS.md
  - id: REQ-1.1-15
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: The problem statement describes the current approach or workaround.
    source: STANDARDS.md
  - id: REQ-1.1-17
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: The problem statement describes what solved looks like.
    source: STANDARDS.md
  - id: REQ-1.2-01
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: "type:feature"
    eval_scope: per-item
    statement: "The §1.2 document progression (problem research, capabilities, PRD, architecture) is followed in order for new products and significant features."
    source: STANDARDS.md
  - id: REQ-1.2-03
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: "type:feature"
    eval_scope: per-item
    statement: "Each §1.2 step is complete when its gate is satisfied, not merely when a document exists."
    source: STANDARDS.md
  - id: REQ-1.2-04
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: "type:feature"
    eval_scope: per-item
    statement: "Each §1.2 step is complete when its gate is satisfied, not merely when a document exists."
    source: STANDARDS.md
  - id: REQ-1.2-05
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: "type:feature"
    eval_scope: per-item
    statement: Producing a document without passing its gate is not completing the step.
    source: STANDARDS.md
  - id: REQ-1.2-06
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: "type:feature"
    eval_scope: per-item
    statement: "Each step's gate is satisfied before the next step begins."
    source: STANDARDS.md
  - id: REQ-1.2-07
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Each document in the §1.2 progression has its gate passed (approval, proceed/no-proceed decision) before the next document begins."
    source: STANDARDS.md
  - id: REQ-1.3-02
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Each phase's measurable milestone is binary verifiable: it either passes or does not, with no judgment required."
    source: STANDARDS.md
  - id: REQ-1.5-01
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "An enforcement tool applying ESE-derived rules to a target repository in a Complicated-domain work session must exempt repositories whose standards-application.md documents an explicit ESE carve-out for the rule in question."
    source: STANDARDS.md
  - id: REQ-1.5-02
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Complex-domain work sessions are exempt from externally applied ESE enforcement rules."
    source: STANDARDS.md
  - id: REQ-2.1-01
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Every work item follows the DISCOVER-DEFINE-DESIGN-BUILD-VERIFY-DOCUMENT-DEPLOY-MONITOR-CLOSE sequence."
    source: STANDARDS.md
  - id: REQ-2.1-02
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Acceptance criteria are written before any implementation work (code, documentation, configuration, or infrastructure) begins."
    source: STANDARDS.md
  - id: REQ-2.1-05
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Tests are written alongside or before the code they cover.
    source: STANDARDS.md
  - id: REQ-2.1-06
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Unit tests pass before the work item advances past VERIFY.
    source: STANDARDS.md
  - id: REQ-2.1-08
    status: active
    mode: deterministic
    scope: document
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: All documentation is written before closing the work item.
    source: STANDARDS.md
  - id: REQ-2.1-12
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: "type:improvement"
    eval_scope: per-item
    statement: Improvement work items record the baseline measurement and method before BUILD begins.
    source: STANDARDS.md
  - id: REQ-2.1-13
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Documentation-only changes: every internal link resolves to a valid target."
    source: STANDARDS.md
  - id: REQ-2.1-15
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Integration tests pass before the work item advances past VERIFY.
    source: STANDARDS.md
  - id: REQ-2.1-17
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "High-RPN failure modes and any with Severity 9-10 have design changes before BUILD begins."
    source: STANDARDS.md
  - id: REQ-2.1-20
    status: active
    mode: deterministic
    scope: discover
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "D1 triage includes an evidence check, a duplicate check, and consultation of the lessons-learned and anti-pattern registries before a triage decision is recorded."
    source: STANDARDS.md
  - id: REQ-2.1-21
    status: active
    mode: deterministic
    scope: discover
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "D1 triage produces exactly one of four decisions: promote to DEFINE, investigate, park with a revisit trigger, or discard with a reason."
    source: STANDARDS.md
  - id: REQ-2.1-24
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "When BUILD discovers DEFINE was incomplete, work returns to DEFINE and AC are updated before BUILD continues."
    source: STANDARDS.md
  - id: REQ-2.1-25
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "When VERIFY reveals DESIGN was wrong, work returns to DESIGN before VERIFY continues."
    source: STANDARDS.md
  - id: REQ-2.1-26
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "When DEPLOY reveals VERIFY was incomplete, work returns to VERIFY before DEPLOY continues."
    source: STANDARDS.md
  - id: REQ-2.1-28
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Documentation-only changes: no formatting defects (corrupt tables, orphaned lines, mismatched heading levels)."
    source: STANDARDS.md
  - id: REQ-2.1-29
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Documentation-only changes: no AI-generated typographic characters (em dashes, en dashes, double-hyphen sentence dashes)."
    source: STANDARDS.md
  - id: REQ-2.1-32
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Documentation-only changes: every template section covers the requirements from its governing standard section."
    source: STANDARDS.md
  - id: REQ-2.1-33
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Documentation-only changes: changelog entry written."
    source: STANDARDS.md
  - id: REQ-2.1-35
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: "type:feature AND type:security"
    eval_scope: per-item
    statement: "The project PFMEA is reviewed when the work item touches authentication, payments, data mutation, or external integrations."
    source: STANDARDS.md
  - id: REQ-2.1-36
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: "type:bug"
    eval_scope: per-item
    statement: "When a bug recurs, the PFMEA is updated with the process failure that allowed recurrence."
    source: STANDARDS.md
  - id: REQ-2.1-37
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Every FMEA failure mode above the RPN threshold has a named control recorded in the FMEA controls summary before the FMEA iteration closes.
    source: STANDARDS.md
  - id: REQ-2.1-38
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Every named control in the FMEA controls summary has either an implemented artifact or a tracked work item before the work item advances past VERIFY.
    source: STANDARDS.md
  - id: REQ-2.1-39
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "No work item is closed while its FMEA has above-threshold failure modes with unimplemented controls unless each has a tracked dependency-linked work item."
    source: STANDARDS.md
  - id: REQ-2.1-40
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Every work period begins by verifying that the prior work period's close obligations were fulfilled: no unfinished deliverables, no unpublished changes, no work items abandoned mid-lifecycle without documented handoff."
    source: STANDARDS.md
  - id: REQ-2.1-41
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: The RPN threshold is defined in the FMEA before failure mode scoring begins.
    source: STANDARDS.md
  - id: REQ-2.1-42
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "After a corrective action is implemented, the failure mode is rescored with the control in place."
    source: STANDARDS.md
  - id: REQ-2.1-43
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "All count references, RPN values, and status claims within an FMEA document are internally consistent at close."
    source: STANDARDS.md
  - id: REQ-2.1-45
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: "type:expedite"
    eval_scope: per-item
    statement: "Expedite work items are not closed until D0 and D1 back-fill documentation is verified complete."
    source: STANDARDS.md
  - id: REQ-2.1-46
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Every markdown table has consistent column counts across all rows within the table.
    source: STANDARDS.md
  - id: REQ-2.1-47
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "FMEA and analysis documents have internally consistent data: iteration counts, RPN values, status claims, and summary tables all reflect the same state."
    source: STANDARDS.md
  - id: REQ-2.1-49
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "FMEA derived sections (High-Severity table, RPN Summary, Controls Summary, Review Checklist) are either auto-generated from the source FM tables or validated for consistency with them before close."
    source: STANDARDS.md
  - id: REQ-2.1-50
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "When a new section is added to a starter template, the project's corresponding instance document is updated in the same work period."
    source: STANDARDS.md
  - id: REQ-2.1-51
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "The project's own artifacts comply with the standards it has adopted: a compliance review that would fail an adopting project also fails the standard's own repository."
    source: STANDARDS.md
  - id: REQ-2.1-53
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "Every checked item in an FMEA Controls Summary that references a script file is verified: the script exists and is called from CI."
    source: STANDARDS.md
  - id: REQ-2.1-54
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: "type:feature OR type:component"
    eval_scope: per-item
    statement: "Before writing an ADR or architecture document: a D2 characterization artifact is either cited in the ADR Context section (path verified to exist) or explicitly stated as not required with rationale."
    source: STANDARDS.md
  - id: REQ-2.2-01
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: A work item has a title.
    source: STANDARDS.md
  - id: REQ-2.2-02
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "A work item either addresses a root cause directly or identifies itself as a symptom fix with a link to the root-cause work item."
    source: STANDARDS.md
  - id: REQ-2.2-05
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Closed work item records are accessible to authorized reviewers for the life of the project.
    source: STANDARDS.md
  - id: REQ-2.2-08
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Acceptance criteria are observable, binary, and measurable: each states a specific condition that is either true or false with no subjective judgment required."
    source: STANDARDS.md
  - id: REQ-2.2-09
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: "type:feature"
    eval_scope: per-item
    statement: "At least one acceptance criterion covers a failure, boundary, or error condition."
    source: STANDARDS.md
  - id: REQ-2.2-10
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: A work item is not claimed until AC are written and agreed.
    source: STANDARDS.md
  - id: REQ-2.2-13
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: A work item has linked dependencies.
    source: STANDARDS.md
  - id: REQ-2.2-14
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: A work item has an assigned owner.
    source: STANDARDS.md
  - id: REQ-2.2-15
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: A work item has a discovery source.
    source: STANDARDS.md
  - id: REQ-2.2-16
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: A work item has a priority.
    source: STANDARDS.md
  - id: REQ-2.2-17
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: A work item has a class of service.
    source: STANDARDS.md
  - id: REQ-2.2-18
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: A work item has a type.
    source: STANDARDS.md
  - id: REQ-2.2-19
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: The work item system maintains lifecycle status.
    source: STANDARDS.md
  - id: REQ-2.2-20
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: The work item system attaches gate evidence in a reviewable form.
    source: STANDARDS.md
  - id: REQ-2.2-21
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: A work item is not claimed until dependencies are identified.
    source: STANDARDS.md
  - id: REQ-2.3-01
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Acceptance criteria are explicitly verified with observable evidence before close.
    source: STANDARDS.md
  - id: REQ-2.3-02
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Tests are written and passing per §6.1 test pyramid (unit, integration, system as applicable)."
    source: STANDARDS.md
  - id: REQ-2.3-03
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Documentation is updated before the work item is closed.
    source: STANDARDS.md
  - id: REQ-2.3-05
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Monitoring is in place for any change that affects production behavior.
    source: STANDARDS.md
  - id: REQ-2.3-06
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: The change is deployed to the live environment.
    source: STANDARDS.md
  - id: REQ-2.3-07
    status: active
    mode: deterministic
    scope: session-end
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: All relevant repositories are pushed and verified up to date with remote.
    source: STANDARDS.md
  - id: REQ-2.3-08
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: The work item record is accessible to authorized reviewers (public system or exported to repository).
    source: STANDARDS.md
  - id: REQ-2.3-10
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Type-conditional close requirements for the work item's type are met in addition to the universal DoD checklist."
    source: STANDARDS.md
  - id: REQ-2.3-11
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: "type:security"
    eval_scope: per-item
    statement: Security type work items have a completed FMEA before close.
    source: STANDARDS.md
  - id: REQ-2.3-12
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: "type:security"
    eval_scope: per-item
    statement: Security type work items have security regression tests per §6.5 before close.
    source: STANDARDS.md
  - id: REQ-2.3-13
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: "type:security"
    eval_scope: per-item
    statement: Security type work items have a security review per §2.5 before close.
    source: STANDARDS.md
  - id: REQ-2.3-14
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: "type:bug"
    eval_scope: per-item
    statement: A regression test is added for every bug fix.
    source: STANDARDS.md
  - id: REQ-2.3-16
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: "type:bug"
    eval_scope: per-item
    statement: "P0 and P1 bug work items have a post-mortem written before close."
    source: STANDARDS.md
  - id: REQ-2.3-17
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: "type:investigation"
    eval_scope: per-item
    statement: Investigation work items have a root cause statement documented before close.
    source: STANDARDS.md
  - id: REQ-2.3-18
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: "type:investigation"
    eval_scope: per-item
    statement: Investigation work items have at least one implementation work item filed before close.
    source: STANDARDS.md
  - id: REQ-2.3-20
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "type:improvement"
    eval_scope: per-item
    statement: Improvement work items verify that the measured result exceeds normal process variation before close.
    source: STANDARDS.md
  - id: REQ-2.3-21
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: "type:component"
    eval_scope: per-item
    statement: Component work items have a completed and reviewed architecture document before close.
    source: STANDARDS.md
  - id: REQ-2.3-22
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: "type:debt"
    eval_scope: per-item
    statement: "Debt work items have the source document (ADR, post-mortem, or code comment) updated to mark debt resolved before close."
    source: STANDARDS.md
  - id: REQ-2.3-23
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: "type:prevention"
    eval_scope: per-item
    statement: "Prevention work items have the source post-mortem Prevention table updated to mark the action closed before close."
    source: STANDARDS.md
  - id: REQ-2.3-24
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: "type:countermeasure"
    eval_scope: per-item
    statement: Countermeasure work items have the source A3 Countermeasures table updated to mark the action closed before close.
    source: STANDARDS.md
  - id: REQ-2.3-26
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: A new person can set it up locally without asking anyone using only the documentation.
    source: STANDARDS.md
  - id: REQ-2.3-29
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: A parent work item is not closed until every constituent work item is either closed or explicitly descoped with documented rationale.
    source: STANDARDS.md
  - id: REQ-2.3-30
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Every acceptance criterion is satisfied before close; no partial satisfaction with deferred items is permitted unless each deferred item has a tracked dependency-linked work item."
    source: STANDARDS.md
  - id: REQ-2.5-01
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Any change expanding attack surface, adding external endpoints, processing new sensitive data, or increasing process permissions has an explicit security review before merge."
    source: STANDARDS.md
  - id: REQ-2.5-02
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-section
    statement: "When delivery health metrics worsen for two consecutive review periods (the minimum evidence for a real trend per statistical process control theory), change size is reduced and reliability work is prioritized before new features."
    source: STANDARDS.md
  - id: REQ-2.5-03
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Any recurring manual operational task whose cumulative cost over a reasonable planning horizon exceeds its one-time automation cost has a filed work item to automate, eliminate, or remove it. Default calibration: three recurrences or 30 minutes per week."
    source: STANDARDS.md
  - id: REQ-2.6-05
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: WIP limits are defined and enforced.
    source: STANDARDS.md
  - id: REQ-3.2-01
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: Code and documentation formatting is enforced by automated tooling.
    source: STANDARDS.md
  - id: REQ-3.2-02
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: New compiler warnings fail the build.
    source: STANDARDS.md
  - id: REQ-3.2-03
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: "Formatting divergence is rejected by the pre-commit hook."
    source: STANDARDS.md
  - id: REQ-3.2-04
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: Markdown documentation follows the CommonMark specification.
    source: STANDARDS.md
  - id: REQ-3.3-01
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: No changes to a component that lacks an architecture doc until the doc exists or an issue is filed to create it.
    source: STANDARDS.md
  - id: REQ-3.4-03
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "Independent testability test passes: the team can test its component in isolation using test doubles for dependencies."
    source: STANDARDS.md
  - id: REQ-4.1-02
    status: active
    mode: deterministic
    scope: document
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "Every documented artifact created from a template contains every required section from that template, or explicitly documents each omitted section inline. A section is required if the template declares it without an optional marker; a section is omitted by recording the omission in the instance file (for example, with an inline comment naming the omitted section)."
    source: STANDARDS.md
  - id: REQ-4.2-01
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: An ADR exists for every qualifying change.
    source: STANDARDS.md
  - id: REQ-4.2-02
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "ADR validation criteria are outcome-based and binary (true or false, not a judgment call) with an event-based assessment trigger."
    source: STANDARDS.md
  - id: REQ-4.2-08
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "When an ADR's dfmea frontmatter field is populated, the named DFMEA file exists and its adr field references this ADR."
    source: STANDARDS.md
  - id: REQ-4.2-09
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "When an ADR's pfmea frontmatter field is populated, the named PFMEA file exists and its adr field references this ADR."
    source: STANDARDS.md
  - id: REQ-4.2-10
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "An ADR that modifies an existing component, API, interface, or standard includes a Per-Document Impact Analysis section."
    source: STANDARDS.md
  - id: REQ-4.2-11
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Each entry in the Per-Document Impact Analysis section states either the required change or an explicit confirmation that no change is required."
    source: STANDARDS.md
  - id: REQ-4.4-01
    status: active
    mode: deterministic
    scope: document
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "Every document with more than three sections has a table of contents (exception: documents following a fixed template where every section is predictable)."
    source: STANDARDS.md
  - id: REQ-4.5-01
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: Every public function and module documents what it does (one sentence).
    source: STANDARDS.md
  - id: REQ-4.5-02
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: No undocumented public functions exist.
    source: STANDARDS.md
  - id: REQ-4.5-05
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: Every public function and module documents why it exists.
    source: STANDARDS.md
  - id: REQ-4.5-06
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: Every public function and module documents what can go wrong (failure modes and edge cases).
    source: STANDARDS.md
  - id: REQ-4.5-07
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: Every public API has example usage documented.
    source: STANDARDS.md
  - id: REQ-4.8-18
    status: active
    mode: deterministic
    scope: document
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: Documentation across all layers is sufficient for a new person to maintain the component without access to the person who built it.
    source: STANDARDS.md
  - id: REQ-4.9-01
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: Section §4.9 does not exceed 150 lines.
    source: STANDARDS.md
  - id: REQ-4.9-03
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: "Every requirement unit is exactly three lines (anchor, tag line, statement)."
    source: STANDARDS.md
  - id: REQ-4.9-04
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Tag positions 1-4 (kind, scope, enforcement, applies-when) are present on every requirement unit."
    source: STANDARDS.md
  - id: REQ-4.9-05
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "REQ-IDs are immutable once published under a versioned CHANGELOG heading."
    source: STANDARDS.md
  - id: REQ-4.9-06
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: "The obligation keyword linter scans every prose line for must, required, shall, block, blocks, gate, cannot and emits unclassified-obligation warnings for lines without a nearby REQ-ID."
    source: STANDARDS.md
  - id: REQ-4.9-07
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: "No sub-clauses in the statement line."
    source: STANDARDS.md
  - id: REQ-4.9-08
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Position 5 (eval-scope) is present when kind is gate and absent otherwise."
    source: STANDARDS.md
  - id: REQ-4.9-09
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Deprecated IDs retain their anchor with a deprecated token.
    source: STANDARDS.md
  - id: REQ-4.9-12
    status: active
    mode: deterministic
    scope: document
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "Requirement statements are first-principles based: they state what must be true without referencing specific tools, products, or proprietary systems unless the requirement governs that specific technology domain."
    source: STANDARDS.md
  - id: REQ-5.1-01
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: The primary branch is always deployable.
    source: STANDARDS.md
  - id: REQ-5.1-02
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: At least one review is completed before merge.
    source: STANDARDS.md
  - id: REQ-5.1-03
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: "Pre-commit checks pass (linting, formatting, type checks) before merge."
    source: STANDARDS.md
  - id: REQ-5.1-04
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: Commit messages are structured and categorized.
    source: STANDARDS.md
  - id: REQ-5.1-05
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: The repository .gitignore excludes build artifacts.
    source: STANDARDS.md
  - id: REQ-5.1-06
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: A future implementor can set up the dev environment using only the documentation.
    source: STANDARDS.md
  - id: REQ-5.1-07
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "All work happens in short-lived branches integrated back before divergence from the primary branch creates material merge conflict risk (default calibration: one to two days)."
    source: STANDARDS.md
  - id: REQ-5.1-08
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Review verifies acceptance criteria are met.
    source: STANDARDS.md
  - id: REQ-5.1-10
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: "The body explains why, not what."
    source: STANDARDS.md
  - id: REQ-5.1-11
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Review verifies tests cover the change.
    source: STANDARDS.md
  - id: REQ-5.1-12
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Review verifies no security issues introduced.
    source: STANDARDS.md
  - id: REQ-5.1-13
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Review verifies code style matches existing patterns.
    source: STANDARDS.md
  - id: REQ-5.1-14
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Review verifies the change does what the description says.
    source: STANDARDS.md
  - id: REQ-5.1-15
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: The repository .gitignore excludes OS files and IDE configs.
    source: STANDARDS.md
  - id: REQ-5.1-16
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: The repository .gitignore excludes dependency directories and compiled output.
    source: STANDARDS.md
  - id: REQ-5.1-17
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: The repository .gitignore excludes secret files.
    source: STANDARDS.md
  - id: REQ-5.1-18
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: A future implementor can understand the change using only the documentation.
    source: STANDARDS.md
  - id: REQ-5.1-19
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: A future implementor can run all tests using only the documentation.
    source: STANDARDS.md
  - id: REQ-5.1-20
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: A future implementor can deploy and verify the change using only the documentation.
    source: STANDARDS.md
  - id: REQ-5.1-21
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: A future implementor knows what monitoring to check using only the documentation.
    source: STANDARDS.md
  - id: REQ-5.10-01
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "Multi-factor authentication or equivalent is required for operator and admin access."
    source: STANDARDS.md
  - id: REQ-5.10-02
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: Services storing or processing sensitive data meet OWASP ASVS Level 1 minimum controls.
    source: STANDARDS.md
  - id: REQ-5.10-04
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: No shared accounts and no default credentials exist in any environment.
    source: STANDARDS.md
  - id: REQ-5.10-05
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: Secrets are never stored in source control.
    source: STANDARDS.md
  - id: REQ-5.10-06
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: All sensitive data is encrypted at rest and in transit (TLS for all network communication).
    source: STANDARDS.md
  - id: REQ-5.10-07
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "Secrets are never stored in logs, screenshots, or crash dumps."
    source: STANDARDS.md
  - id: REQ-5.2-01
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "All dependencies are explicitly declared, version-pinned, and vulnerability-scanned on every CI pass."
    source: STANDARDS.md
  - id: REQ-5.2-02
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: Dependency licenses are audited at minimum annually.
    source: STANDARDS.md
  - id: REQ-5.2-03
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: No undeclared global tools are required for development or deployment.
    source: STANDARDS.md
  - id: REQ-5.2-04
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "High-severity findings block merge."
    source: STANDARDS.md
  - id: REQ-5.4-01
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "Every external call (HTTP, database, queue, third-party API) has a defined timeout."
    source: STANDARDS.md
  - id: REQ-5.4-03
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: Any operation that retries on failure uses exponential backoff with jitter.
    source: STANDARDS.md
  - id: REQ-5.4-04
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "All long-running processes tolerate restart without corrupting state or duplicating irreversible work."
    source: STANDARDS.md
  - id: REQ-5.5-01
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: An automated CI pipeline runs on every proposed change before merge.
    source: STANDARDS.md
  - id: REQ-5.5-02
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "Rollback trigger and strategy are pre-defined before any production deployment."
    source: STANDARDS.md
  - id: REQ-5.5-03
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: Every passing build on the primary branch is deployable to production.
    source: STANDARDS.md
  - id: REQ-5.5-06
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: Pipeline fails visibly on any error.
    source: STANDARDS.md
  - id: REQ-5.5-07
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: The CI pipeline compiles the code.
    source: STANDARDS.md
  - id: REQ-5.5-08
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: The CI pipeline runs the full test suite.
    source: STANDARDS.md
  - id: REQ-5.5-09
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: The CI pipeline runs a dependency vulnerability scan.
    source: STANDARDS.md
  - id: REQ-5.5-10
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: "The CI pipeline runs pre-commit gates."
    source: STANDARDS.md
  - id: REQ-5.8-01
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: APIs follow semantic versioning.
    source: STANDARDS.md
  - id: REQ-5.8-02
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "Breaking changes require a major version increment, a documented deprecation period, and old versions remain functional through the deprecation window."
    source: STANDARDS.md
  - id: REQ-6.1-01
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Every function with logic (branching, looping, computation, state transformation, error handling) has a unit test."
    source: STANDARDS.md
  - id: REQ-6.1-02
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Integration tests (multiple modules, may use real DB, runs on CI) pass before the work item advances."
    source: STANDARDS.md
  - id: REQ-6.1-03
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Performance tests assert on timing for any feature with a latency requirement.
    source: STANDARDS.md
  - id: REQ-6.1-04
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Every fixed bug has a regression test.
    source: STANDARDS.md
  - id: REQ-6.1-05
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Load tests verify concurrent usage for any service with external users.
    source: STANDARDS.md
  - id: REQ-6.3-01
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "Every output type (UI, API, CLI, errors, logs, notifications, reports) is verified for correct communication."
    source: STANDARDS.md
  - id: REQ-6.3-02
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "Every output type is intentional, complete, and clear."
    source: STANDARDS.md
  - id: REQ-6.3-04
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "User-facing output is usable by people with disabilities (keyboard navigation, accessible labels, color not sole channel)."
    source: STANDARDS.md
  - id: REQ-6.3-06
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: Every output type is verified for correct formatting.
    source: STANDARDS.md
  - id: REQ-6.3-07
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: Every output type is verified for error handling.
    source: STANDARDS.md
  - id: REQ-6.3-08
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: Every output type is verified for edge cases.
    source: STANDARDS.md
  - id: REQ-6.3-09
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "Every output type is verified for cross-environment consistency."
    source: STANDARDS.md
  - id: REQ-6.4-01
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Work stops when a defect is detected during development.
    source: STANDARDS.md
  - id: REQ-6.4-02
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: No defect passes downstream to the next stage.
    source: STANDARDS.md
  - id: REQ-6.5-01
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "type:security"
    eval_scope: per-item
    statement: "Security-affecting changes have regression tests covering unauthorized access."
    source: STANDARDS.md
  - id: REQ-6.5-02
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: all
    eval_scope: per-commit
    statement: Security regression tests are permanent and run on every CI pass.
    source: STANDARDS.md
  - id: REQ-6.5-03
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "type:security"
    eval_scope: per-item
    statement: "Security-affecting changes have regression tests covering injection."
    source: STANDARDS.md
  - id: REQ-6.5-04
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "type:security"
    eval_scope: per-item
    statement: "Security-affecting changes have regression tests covering path traversal."
    source: STANDARDS.md
  - id: REQ-6.5-05
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "type:security"
    eval_scope: per-item
    statement: "Security-affecting changes have regression tests covering SSRF."
    source: STANDARDS.md
  - id: REQ-6.5-06
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "type:security"
    eval_scope: per-item
    statement: "Security-affecting changes have regression tests covering data leakage."
    source: STANDARDS.md
  - id: REQ-7.1-01
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "Every always-on service has a health check."
    source: STANDARDS.md
  - id: REQ-7.1-04
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: The health check alerts when silent beyond a defined threshold.
    source: STANDARDS.md
  - id: REQ-7.1-05
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: The health check is functionally meaningful (not just a process ping).
    source: STANDARDS.md
  - id: REQ-7.3-01
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: Every consequential action is logged with who initiated it.
    source: STANDARDS.md
  - id: REQ-7.3-02
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: Both blocked and permitted actions are logged.
    source: STANDARDS.md
  - id: REQ-7.5-02
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-section
    statement: "When a service's error budget consumption rate, extrapolated forward, would exhaust the remaining budget before the measurement period ends, reliability work takes priority over new feature work until the trend corrects. The generating principle: error budgets balance reliability investment against feature velocity; when consumption outpaces the planned rate, the balance has tipped toward unreliability. The simplest check: if more than half the budget is consumed before the period's midpoint, the rate exceeds plan."
    source: STANDARDS.md
  - id: REQ-7.6-01
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "Every production service produces correlated traces, metrics, and structured logs."
    source: STANDARDS.md
  - id: REQ-7.6-02
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: Sensitive payloads are redacted before emission.
    source: STANDARDS.md
  - id: REQ-8.1-01
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: "type:bug"
    eval_scope: per-item
    statement: "Every incident is classified per the taxonomy before the post-mortem is written and produces at least one regression case."
    source: STANDARDS.md
  - id: REQ-8.2-10
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Every prevention action in a post-mortem has a tracked work item (type=prevention) that is not closed until the action is implemented."
    source: STANDARDS.md
  - id: REQ-8.2-11
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: "type:prevention"
    eval_scope: per-item
    statement: "A prevention work item includes evidence that the implemented action prevents the original failure mode, not just that the action was completed."
    source: STANDARDS.md
  - id: REQ-8.3-01
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "The lessons-learned registry is consulted before starting any new feature."
    source: STANDARDS.md
  - id: REQ-8.3-03
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: "type:bug"
    eval_scope: per-item
    statement: "Every post-mortem lesson is added to the lessons-learned registry."
    source: STANDARDS.md
  - id: REQ-8.6-01
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Acknowledged technical debt is tracked as type=debt work items."
    source: STANDARDS.md
  - id: REQ-8.6-02
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: "type:debt"
    eval_scope: per-item
    statement: "When a debt work item is closed, the source document (ADR, post-mortem, or code comment) is updated with the work item ID."
    source: STANDARDS.md
  - id: REQ-ADD-AI-01
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-item
    statement: "Every downstream consumer of AI output has a named fallback action (default value, human escalation, rejection with explanation, or safe-state hold) documented in its architecture document."
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-02
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-item
    statement: "Every AI-consuming component distinguishes confidence score from correctness."
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-03
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-item
    statement: "Every AI-driven action type is classified by autonomy level (informational, assisted, automated, autonomous)."
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-05
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-artifact
    statement: A regression threshold is defined in a committed file before evaluation begins.
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-06
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-artifact
    statement: "Training and fine-tuning data has documented provenance."
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-07
    status: active
    mode: deterministic
    scope: monitor
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-artifact
    statement: AI systems monitor output quality metrics.
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-09
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-item
    statement: "The architecture reflects that high-confidence wrong answers are more dangerous than low-confidence answers that trigger fallback."
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-10
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-item
    statement: Mixed levels require explicit boundaries.
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-11
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-artifact
    statement: CI fails (not warns) when results breach the threshold.
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-24
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-artifact
    statement: Training data contains no PII without documented consent and legal basis.
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-25
    status: active
    mode: deterministic
    scope: monitor
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-artifact
    statement: Data drift between training data and production inputs is monitored with a defined threshold.
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-26
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-artifact
    statement: Training data retention policies match production data retention policies.
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-27
    status: active
    mode: deterministic
    scope: monitor
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-artifact
    statement: AI systems monitor input distribution drift.
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-28
    status: active
    mode: deterministic
    scope: monitor
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-artifact
    statement: AI systems monitor confidence distribution shifts.
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-29
    status: active
    mode: deterministic
    scope: monitor
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-artifact
    statement: AI systems monitor anomalous outputs.
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-30
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-item
    statement: "The F1 threshold for LLM-generated enforcement rule activation is set and committed before evaluation begins."
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-31
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-artifact
    statement: "An LLM-generated enforcement gate rule has been evaluated against a minimum of 2 independent labeled sample sets before promotion from inert to active."
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-32
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-artifact
    statement: "Each of the 2 required evaluation runs achieves F1 >= 0.85 independently before the rule is promoted from inert to active."
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-33
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-artifact
    statement: "The promotion commit for an LLM-generated rule records the F1 score from each of the two required evaluation runs."
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-34
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-artifact
    statement: "The promotion commit for an LLM-generated rule records the labeled set reference used in each evaluation run."
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-AI-35
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: "addendum:AI"
    eval_scope: per-artifact
    statement: "The promotion commit for an LLM-generated rule includes explicit confirmation that the F1 threshold was set before evaluation began."
    source: docs/addenda/ai-ml.md
  - id: REQ-ADD-CI-01
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: "Value stream mapping produces a current-state map with active time and wait time sourced from at least 10 completed work items."
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-02
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: Kaizen event produces a before/after measurement of the target metric.
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-03
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI AND type:improvement"
    eval_scope: per-item
    statement: "Improvement claims are verified by process capability measurement: the claimed improvement exceeds normal process variation (common cause vs special cause distinction made before declaring improvement verified)."
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-04
    status: active
    mode: deterministic
    scope: discover
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: Gemba (direct observation of current state from actual artifacts) is completed before any improvement hypothesis is formed.
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-07
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: At least one improvement work item is filed with a measurable before/after target.
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-08
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: No measurement improvement without data.
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-09
    status: active
    mode: deterministic
    scope: discover
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: "Observations are written, not inferred."
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-13
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: The improvement is implemented (not planned) by the end of the event.
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-14
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: "The result (target met or missed, with the actual measured value) is recorded in the lessons-learned registry."
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-16
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: The team has documented in writing whether the current state is special cause (specific cause investigated and recorded) or common cause (process change planned or in progress).
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-17
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: "Every user-facing function has at least one failure mode documented."
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-18
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: "All RPN 100+ failure modes have a named owner and a specific action (not \"under review\")."
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-20
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: Every recurring pattern is matched to a named TIMWOODS category.
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-21
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: Each top pattern is filed as a work item with a measurable improvement target.
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-22
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: A single stage is identified as the constraint by name.
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-23
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: Subordination actions (adjusted WIP limits or prioritization rules) are in place and documented.
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-25
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: "The rope limit is enforced: new pipeline intake is gated on buffer consumption, not on available capacity at the first stage."
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-28
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: Median and 95th percentile are recorded for all identified setup event types.
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-29
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: "The new baseline is documented in the project's setup documentation or standards-application.md."
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-33
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: Written observations from direct production log review (not a summary or alert rollup) are recorded with a timestamp.
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-35
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: "No VSM, Kaizen pre-work, or A3 current-state section has been started before all four observation sets are written."
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-36
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: Every customer need has at least one Strong or Weak relationship to a tech spec.
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-37
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: All conflicts between specs are explicitly marked and each conflict has a recorded resolution decision.
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-38
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: "All five columns have named, specific entries with no blanks and no \"TBD\" values."
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-39
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: "What is outside the Suppliers-to-Customers boundary is stated explicitly in writing and agreed to by the team."
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-61
    status: active
    mode: deterministic
    scope: close
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: "Kaizen events that improve user-facing behavior close the §2.7 feedback loop by notifying affected users."
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CI-62
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:CI"
    eval_scope: per-item
    statement: "FMEA derived sections (High-Severity table, RPN Summary, Controls Summary, Review Checklist) are consistent with source FM tables at close."
    source: docs/addenda/continuous-improvement.md
  - id: REQ-ADD-CTR-01
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:CTR"
    eval_scope: per-commit
    statement: Image scanning runs on every build.
    source: docs/addenda/containerized-systems.md
  - id: REQ-ADD-CTR-02
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:CTR"
    eval_scope: per-artifact
    statement: "Liveness probe is configured with documented endpoint, thresholds, and timing."
    source: docs/addenda/containerized-systems.md
  - id: REQ-ADD-CTR-03
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:CTR"
    eval_scope: per-artifact
    statement: Resource limits (CPU and memory) are set for every container.
    source: docs/addenda/containerized-systems.md
  - id: REQ-ADD-CTR-04
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:CTR"
    eval_scope: per-artifact
    statement: "Container runs as non-root user."
    source: docs/addenda/containerized-systems.md
  - id: REQ-ADD-CTR-05
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:CTR"
    eval_scope: per-commit
    statement: Critical and High severity findings block deployment.
    source: docs/addenda/containerized-systems.md
  - id: REQ-ADD-CTR-06
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:CTR"
    eval_scope: per-commit
    statement: Medium findings triaged within 30 days.
    source: docs/addenda/containerized-systems.md
  - id: REQ-ADD-CTR-07
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:CTR"
    eval_scope: per-commit
    statement: "Weekly scheduled re-scans active."
    source: docs/addenda/containerized-systems.md
  - id: REQ-ADD-CTR-08
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:CTR"
    eval_scope: per-artifact
    statement: Health probes are active before traffic is routed.
    source: docs/addenda/containerized-systems.md
  - id: REQ-ADD-CTR-09
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:CTR"
    eval_scope: per-artifact
    statement: "Memory request is at least 50% of limit."
    source: docs/addenda/containerized-systems.md
  - id: REQ-ADD-CTR-10
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:CTR"
    eval_scope: per-artifact
    statement: "CPU request is at least 25% of limit."
    source: docs/addenda/containerized-systems.md
  - id: REQ-ADD-CTR-11
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:CTR"
    eval_scope: per-artifact
    statement: "All log output is structured (JSON or equivalent machine-parseable format)."
    source: docs/addenda/containerized-systems.md
  - id: REQ-ADD-CTR-17
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:CTR"
    eval_scope: per-artifact
    statement: "Readiness probe is configured with documented endpoint, thresholds, and timing."
    source: docs/addenda/containerized-systems.md
  - id: REQ-ADD-CTR-18
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:CTR"
    eval_scope: per-artifact
    statement: "Startup probe is configured with documented endpoint, thresholds, and timing."
    source: docs/addenda/containerized-systems.md
  - id: REQ-ADD-CTR-19
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:CTR"
    eval_scope: per-artifact
    statement: "Container has read-only root filesystem."
    source: docs/addenda/containerized-systems.md
  - id: REQ-ADD-CTR-20
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:CTR"
    eval_scope: per-artifact
    statement: All capabilities dropped (add back only what is explicitly required).
    source: docs/addenda/containerized-systems.md
  - id: REQ-ADD-CTR-21
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:CTR"
    eval_scope: per-artifact
    statement: No privilege escalation allowed.
    source: docs/addenda/containerized-systems.md
  - id: REQ-ADD-EVT-01
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:EVT"
    eval_scope: per-artifact
    statement: Every event type has a versioned schema.
    source: docs/addenda/event-driven.md
  - id: REQ-ADD-EVT-02
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:EVT"
    eval_scope: per-item
    statement: Consumers ignore unknown fields (tolerant reader pattern).
    source: docs/addenda/event-driven.md
  - id: REQ-ADD-EVT-03
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:EVT"
    eval_scope: per-item
    statement: Every consumer handles duplicate message delivery without producing duplicate effects.
    source: docs/addenda/event-driven.md
  - id: REQ-ADD-EVT-06
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:EVT"
    eval_scope: per-artifact
    statement: All broker connections use TLS.
    source: docs/addenda/event-driven.md
  - id: REQ-ADD-EVT-07
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:EVT"
    eval_scope: per-artifact
    statement: Schema compatibility is enforced mechanically via a schema registry.
    source: docs/addenda/event-driven.md
  - id: REQ-ADD-EVT-08
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:EVT"
    eval_scope: per-artifact
    statement: Breaking changes follow the §5.8 deprecation policy.
    source: docs/addenda/event-driven.md
  - id: REQ-ADD-EVT-09
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:EVT"
    eval_scope: per-item
    statement: "Schema evolution is add-only."
    source: docs/addenda/event-driven.md
  - id: REQ-ADD-EVT-10
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:EVT"
    eval_scope: per-item
    statement: "Fields are never removed, renamed, or re-typed in place."
    source: docs/addenda/event-driven.md
  - id: REQ-ADD-EVT-11
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:EVT"
    eval_scope: per-item
    statement: The architecture document names the specific idempotency mechanism and where state is stored.
    source: docs/addenda/event-driven.md
  - id: REQ-ADD-EVT-14
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:EVT"
    eval_scope: per-artifact
    statement: Every producer and consumer authenticates.
    source: docs/addenda/event-driven.md
  - id: REQ-ADD-EVT-15
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:EVT"
    eval_scope: per-artifact
    statement: "ACLs enforce least-privilege."
    source: docs/addenda/event-driven.md
  - id: REQ-ADD-EVT-16
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:EVT"
    eval_scope: per-artifact
    statement: Credentials rotate on a defined schedule and immediately on suspected compromise.
    source: docs/addenda/event-driven.md
  - id: REQ-ADD-MS-01
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: "addendum:MS"
    eval_scope: per-item
    statement: Every request crossing a service boundary is authenticated.
    source: docs/addenda/multi-service.md
  - id: REQ-ADD-MS-02
    status: active
    mode: deterministic
    scope: verify
    enforcement: hard
    applies_when: "addendum:MS"
    eval_scope: per-item
    statement: "Every service is independently deployable, independently testable with test doubles, and capable of running against the previous and next version of any dependency."
    source: docs/addenda/multi-service.md
  - id: REQ-ADD-MS-03
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:MS"
    eval_scope: per-artifact
    statement: "Consumer-driven contract tests exist for every public API endpoint consumed by an external team."
    source: docs/addenda/multi-service.md
  - id: REQ-ADD-MS-04
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:MS"
    eval_scope: per-item
    statement: "Every outbound call has a defined timeout, circuit breaker with failure threshold, and defined fallback behavior when the circuit is open."
    source: docs/addenda/multi-service.md
  - id: REQ-ADD-MS-05
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:MS"
    eval_scope: per-artifact
    statement: Every request crossing a service boundary propagates a correlation ID.
    source: docs/addenda/multi-service.md
  - id: REQ-ADD-MS-07
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: "addendum:MS"
    eval_scope: per-item
    statement: "The authentication mechanism (mTLS, token, API key) and authorization model are documented for each interface."
    source: docs/addenda/multi-service.md
  - id: REQ-ADD-MS-08
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:MS"
    eval_scope: per-artifact
    statement: Provider CI fails if a consumer contract is broken.
    source: docs/addenda/multi-service.md
  - id: REQ-ADD-MS-09
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:MS"
    eval_scope: per-artifact
    statement: All spans in a request trace are linkable across all services.
    source: docs/addenda/multi-service.md
  - id: REQ-ADD-MT-03
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: "addendum:MT"
    eval_scope: per-item
    statement: "An RFC is written before implementation begins when any trigger condition is true (API change, behavior change, capability removal, coordinated deploy, auth change, shared schema change)."
    source: docs/addenda/multi-team.md
  - id: REQ-ADD-MT-07
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: "addendum:MT"
    eval_scope: per-item
    statement: "Changes affecting another team's work or timelines are communicated before work begins: what is changing, when, what action required, and who to contact."
    source: docs/addenda/multi-team.md
  - id: REQ-ADD-WEB-02
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-artifact
    statement: Web interfaces conform to WCAG 2.2 Level AA minimum.
    source: docs/addenda/web-applications.md
  - id: REQ-ADD-WEB-03
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-commit
    statement: An automated accessibility scan runs on every pull request.
    source: docs/addenda/web-applications.md
  - id: REQ-ADD-WEB-04
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-artifact
    statement: "Content-Security-Policy header is emitted."
    source: docs/addenda/web-applications.md
  - id: REQ-ADD-WEB-05
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-artifact
    statement: Keyboard navigation meets AA thresholds.
    source: docs/addenda/web-applications.md
  - id: REQ-ADD-WEB-06
    status: active
    mode: deterministic
    scope: commit
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-commit
    statement: "Automated pass is a gate, not a certification."
    source: docs/addenda/web-applications.md
  - id: REQ-ADD-WEB-07
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-artifact
    statement: Verified on every deployment.
    source: docs/addenda/web-applications.md
  - id: REQ-ADD-WEB-08
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-artifact
    statement: "All user-visible strings are externalized through a localization function."
    source: docs/addenda/web-applications.md
  - id: REQ-ADD-WEB-09
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-artifact
    statement: "No user-visible string is assembled by string concatenation in code."
    source: docs/addenda/web-applications.md
  - id: REQ-ADD-WEB-10
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-artifact
    statement: CSP disallows inline scripts except where explicitly required and documented.
    source: docs/addenda/web-applications.md
  - id: REQ-ADD-WEB-29
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-artifact
    statement: Focus management meets AA thresholds.
    source: docs/addenda/web-applications.md
  - id: REQ-ADD-WEB-30
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-artifact
    statement: Screen reader compatibility meets AA thresholds.
    source: docs/addenda/web-applications.md
  - id: REQ-ADD-WEB-31
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-artifact
    statement: Color is not the sole channel for conveying information.
    source: docs/addenda/web-applications.md
  - id: REQ-ADD-WEB-32
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-artifact
    statement: Text contrast meets AA thresholds.
    source: docs/addenda/web-applications.md
  - id: REQ-ADD-WEB-33
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-artifact
    statement: "All user-visible dates and numbers are externalized through a localization function."
    source: docs/addenda/web-applications.md
  - id: REQ-ADD-WEB-34
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-artifact
    statement: "All user-visible currencies are externalized through a localization function."
    source: docs/addenda/web-applications.md
  - id: REQ-ADD-WEB-35
    status: active
    mode: deterministic
    scope: build
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-artifact
    statement: "All user-visible plural forms are externalized through a localization function."
    source: docs/addenda/web-applications.md
  - id: REQ-ADD-WEB-36
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-artifact
    statement: "X-Frame-Options header is emitted."
    source: docs/addenda/web-applications.md
  - id: REQ-ADD-WEB-37
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-artifact
    statement: "X-Content-Type-Options header is emitted."
    source: docs/addenda/web-applications.md
  - id: REQ-ADD-WEB-38
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-artifact
    statement: "Strict-Transport-Security (HSTS) header is emitted."
    source: docs/addenda/web-applications.md
  - id: REQ-ADD-WEB-39
    status: active
    mode: deterministic
    scope: deploy
    enforcement: hard
    applies_when: "addendum:WEB"
    eval_scope: per-artifact
    statement: "Referrer-Policy header is emitted."
    source: docs/addenda/web-applications.md
  - id: REQ-ADO-02
    status: active
    mode: deterministic
    scope: continuous
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "After every ESE version update, all four steps are completed: read the CHANGELOG, audit all template-based artifacts, run a compliance review, file a work item for every gap found."
    source: docs/adoption.md
  - id: REQ-ADO-03
    status: active
    mode: deterministic
    scope: define
    enforcement: hard
    applies_when: all
    eval_scope: per-artifact
    statement: "First steps after adoption are completed in order: copy starters, set up repo structure, identify applicable addenda, define intake channel and triage cadence, file issues for every gap, define compliance review cadence, and process first signal through the §2.1 decision tree."
    source: docs/adoption.md
  - id: REQ-TPL-09
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Architecture document goals are measurable with a specific metric, threshold, and measurement method."
    source: templates/architecture-doc.md
  - id: REQ-TPL-11
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: "Every external dependency states what happens when it is unavailable: timeout, retry, and fallback behavior."
    source: templates/architecture-doc.md
  - id: REQ-TPL-12
    status: active
    mode: deterministic
    scope: design
    enforcement: hard
    applies_when: all
    eval_scope: per-item
    statement: Each gap in the current vs target state table has a filed §2.2 work item ID.
    source: templates/architecture-doc.md
